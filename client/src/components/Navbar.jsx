function Navbar() {
  return (
    <header className="navbar">
      <div className="logo">Yuka Coffee</div>

      <nav className="nav-links">
        <a href="#menu">Menu</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>
  )
}

export default Navbar