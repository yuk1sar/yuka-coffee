# Local PostgreSQL. Keep real values only in server/.env.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=yuka_coffee
DB_USER=postgres
DB_PASSWORD=
JWT_SECRET=
PORT=5000
# For Neon, set DATABASE_URL. It takes precedence over DB_*.
# DATABASE_URL=
# On Render set NODE_ENV=production and CORS_ORIGINS to the Vercel origin.
# CORS_ORIGINS=https://your-project.vercel.app
